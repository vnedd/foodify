import Slider from "./slider/slider";

const Hero = () => {
  return (
    <section className="flex w-full flex-row-reverse p-4 pr-0 bg-secondary dark:bg-secondary/40 rounded-xl">
      <Slider />
    </section>
  );
};

export default Hero;
