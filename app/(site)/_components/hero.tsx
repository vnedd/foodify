import Slider from "./slider/slider";

const Hero = () => {
  return (
    <section className="flex w-full flex-row-reverse p-4 pr-0 bg-secondary/30 dark:bg-secondary50 rounded-xl">
      <Slider />
    </section>
  );
};

export default Hero;
